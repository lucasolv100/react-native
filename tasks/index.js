/**
 * @format
 */

import {AppRegistry} from 'react-native';
import TaskLit from './src/screens/TaskList';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => TaskLit);
